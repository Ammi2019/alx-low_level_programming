#include <stdio.h>

/**
 * main -a program that prints the lowercase alphabet in reverse
 *
 * Return: aphabet to stdout in reverse
 */

int main(void)
{
	char c = a;
	for (a...z)
	{
		putchar(c);
		c--;
		putchar('\n');
	}
	return (0);
}
