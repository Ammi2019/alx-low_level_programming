#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - entry point
 * Return: Always 0 (success)
 */

int main(void)
{

	int d;

	for (d = '0'; d <= '9'; d++)
	putchar(d);
	putchar('\n');

	return (0);
}
