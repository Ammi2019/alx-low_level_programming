#include <stdio.h>

/**
* main - print alphabet in lower case
* Return: void
*/

int main(void)
{

	char alph;
	for (alph = 'a'; alph <= 'z'; alph++);

	putchar("alph");
	putchar('\n');

	return (0);
}
