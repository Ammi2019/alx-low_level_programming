#include <stdio.h>
#include "main.h"

/**
 * mul - a function that multiplies two integers
 * 
 * Return: multip,of two intergers
 */
int mul(int a, int b)
{
	int _mul;
	_mul = a * b;
	if (_mul)
	{
		return (_mul);
	}
	return(0);
}
