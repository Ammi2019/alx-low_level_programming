#include <stdio.h>
#include "main.h"

/**
 * _isdigit - a function that checks for a digit (0 through 9)
 * 
 * Return: 1 if digit and 0 other wise
 */

int _isdigit(int c)
{
	c = 0;
	if((c = 0) && (c <= 9))
	{
		return (1);
	}
	else 
		return (0);
}
