#include "main.h"

/**
 * _islower -checks lower case
 * Return: 1 for any alphabet character, 0 for otherwise
 */

int _islower(int c)

{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (0);

}
