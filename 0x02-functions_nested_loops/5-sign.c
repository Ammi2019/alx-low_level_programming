#include "main.h"

/**
* print_sign - sign of a number
* Return: 1 if positive, 0 if zero, -1 if negative
*/

int print_sign(int n)

{
	int n;
	
	if (n < 0)
	{
	_putchar(n);
	}
	else if (n > 0)
	_putchar()
	return ();

	else 
	_putchar();
	return ()

}

